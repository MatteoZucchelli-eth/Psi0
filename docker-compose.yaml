services:
  psi0-container:
    image: psi0-environment:latest
    volumes:
      - ./:/workspace
    command: sleep infinity
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]